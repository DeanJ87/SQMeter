import type { SensorData, SystemStatus, Config, GithubRelease } from "../types";

const jitter = (base: number, range: number) =>
  base + (Math.random() - 0.5) * range;

export function generateSensorData(): SensorData {
  const sqm = jitter(21.45, 0.08);
  const lux = Math.pow(10, (12.59 - sqm) / 2.5);

  return {
    dataTimestamp: Date.now(),
    lightSensor: {
      lux: parseFloat(lux.toFixed(4)),
      visible: Math.round(jitter(312, 10)),
      infrared: Math.round(jitter(48, 4)),
      full: Math.round(jitter(360, 12)),
      status: 0,
    },
    skyQuality: {
      sqm: parseFloat(sqm.toFixed(2)),
      nelm: parseFloat(jitter(6.18, 0.05).toFixed(2)),
      bortle: 2,
      description: "Typical truly dark site",
    },
    environment: {
      temperature: parseFloat(jitter(12.4, 0.2).toFixed(1)),
      humidity: parseFloat(jitter(64.8, 0.5).toFixed(1)),
      pressure: parseFloat(jitter(1013.25, 0.3).toFixed(2)),
      dewpoint: parseFloat(jitter(6.1, 0.2).toFixed(1)),
      status: 0,
    },
    irTemperature: {
      objectTemp: parseFloat(jitter(-24.8, 0.4).toFixed(1)),
      ambientTemp: parseFloat(jitter(12.4, 0.1).toFixed(1)),
      status: 0,
    },
    cloudConditions: {
      temperatureDelta: parseFloat(jitter(-37.2, 0.5).toFixed(1)),
      correctedDelta: parseFloat(jitter(-32.8, 0.4).toFixed(1)),
      cloudCoverPercent: parseFloat(jitter(3.0, 1.0).toFixed(1)),
      condition: 1,
      description: "Clear",
      humidityUsed: 64.8,
    },
    gps: {
      hasFix: true,
      satellites: 9,
      latitude: 51.5074,
      longitude: -0.1278,
      altitude: 42.0,
      hdop: 1.1,
      age: Math.round(jitter(800, 100)),
    },
    rainSensor: {
      enabled: true,
      sensor: "hydreon_rg15",
      initialized: true,
      online: true,
      stale: false,
      status: 0,
      timestamp: Date.now(),
      ageMs: 0,
      isRaining: true,
      raining: true,
      acc: parseFloat(jitter(0.2, 0.1).toFixed(2)),
      eventAcc: parseFloat(jitter(2.4, 0.2).toFixed(2)),
      totalAcc: parseFloat(jitter(12.6, 0.1).toFixed(1)),
      rInt: parseFloat(jitter(2.4, 0.3).toFixed(1)),
      accumulation_since_last_read: 0.2,
      event_accumulation: 0.4,
      local_event_accumulation: 0.4,
      hydreon_event_accumulation: 2.4,
      total_accumulation: 12.6,
      rain_intensity: 2.4,
      lensBad: false,
      emSat: false,
      uart: {
        configured: true,
        opened: true,
        rx_pin: 18,
        tx_pin: 19,
        baud_rate: 9600,
        uart_port: 1,
        mode: "polling",
        resolution: "high",
        units: "metric",
        debug_uart: false,
        poll_interval_ms: 5000,
        rain_clear_delay_ms: 900000,
        daily_reset_enabled: true,
        daily_reset_hour: 0,
        daily_reset_minute: 0,
        last_command: "R",
        last_command_ms: Date.now() - 120,
        last_bytes_written: 2,
        expected_ack: null,
        last_ack: "m",
        last_ack_ms: Date.now() - 160,
        last_raw_response: "Acc 0.00 mm, EventAcc 0.00 mm, TotalAcc 1.24 mm, RInt 0.00 mm/h",
        last_response_ms: Date.now() - 40,
        last_error: null,
        timeouts: 0,
        parse_errors: 0,
        successful_reads: 42,
        response_timeout_ms: 500,
        stale_timeout_ms: 30000,
        last_poll_ms: Date.now() - 40,
        last_poll_age_ms: 40,
        last_rain_detected_ms: Date.now() - 2000,
        last_rain_detected_age_ms: 2000,
        last_total_reset_ms: Date.now() - 3600000,
        last_total_reset_age_ms: 3600000,
        last_reboot_command_ms: null,
        last_reboot_command_age_ms: null,
        last_health_check_ms: Date.now() - 180000,
        last_health_check_age_ms: 180000,
        last_status_line: "SW 1.000 2020.07.06",
        software_version: "1.000",
        software_build_date: "2020.07.06",
        reset_reason: "N",
        power_on_days: 13,
        emitter_1: 9,
        emitter_2: 10,
        emitter_total: 19,
        last_response_age_ms: 40,
        last_successful_read_ms: Date.now() - 40,
        last_successful_read_age_ms: 40,
      },
    },
  };
}

export const mockStatus: SystemStatus = {
  firmware: {
    name: "SQMeter",
    version: "0.0.1",
    buildDate: "Apr 24 2026",
    buildTime: "12:00:00",
  },
  uptime: 7200,
  freeHeap: 214320,
  heapSize: 327680,
  cpuFreqMHz: 240,
  flashSize: 4194304,
  sketchSize: 1245184,
  freeSketchSpace: 917504,
  fsTotal: 524288,
  fsUsed: 204800,
  partitions: {
    runningSlot: "app0",
    runningAddress: 0x10000,
    runningSize: 1572864,
    bootSlot: "app0",
    nextSlot: "app1",
    nextSize: 1572864,
    nvs: {
      usedEntries: 12,
      freeEntries: 488,
      totalEntries: 500,
      namespaceCount: 1,
    },
    fsAddress: 0x310000,
    fsSize: 524288,
  },
  time: {
    iso: new Date().toISOString(),
    timezone: "GMT0",
  },
  ntp: {
    enabled: true,
    synced: true,
    status: 1,
    lastSync: Date.now() - 3600000,
    nextSync: Date.now() + 3600000,
    drift: 0.003,
    server: "pool.ntp.org",
    activeSource: 1,
    gpsEnabled: true,
    gpsHasFix: true,
    gpsTimeUTC: new Date().toISOString(),
    gpsSatellites: 9,
  },
  wifi: {
    connected: true,
    ssid: "DarkSkyLab",
    ip: "192.168.1.42",
    rssi: -58,
    mac: "AA:BB:CC:DD:EE:FF",
  },
  mqtt: {
    enabled: true,
    connected: true,
    state: 0,
    lastPublish: Date.now() - 60000,
    lastReconnectAttempt: 0,
    broker: "mqtt.example.com",
    port: 1883,
    topic: "sqmeter/data",
  },
  sensors: {
    tsl2591: { initialized: true, status: 0, lastUpdate: Date.now() - 5000 },
    bme280: { initialized: true, status: 0, lastUpdate: Date.now() - 5000 },
    mlx90614: { initialized: true, status: 0, lastUpdate: Date.now() - 5000 },
    gps: { initialized: true, status: 0, lastUpdate: Date.now() - 1000 },
    rg15: {
      enabled: true,
      initialized: true,
      online: true,
      stale: false,
      state: "online",
      status: 0,
      lastUpdate: Date.now() - 5000,
      timestamp: Date.now() - 5000,
      ageMs: 5000,
      isRaining: true,
      raining: true,
      acc: 0.2,
      eventAcc: 2.4,
      totalAcc: 12.6,
      rInt: 2.4,
      accumulation_since_last_read: 0.2,
      event_accumulation: 0.4,
      local_event_accumulation: 0.4,
      hydreon_event_accumulation: 2.4,
      total_accumulation: 12.6,
      rain_intensity: 2.4,
      lensBad: false,
      emSat: false,
      uart: {
        configured: true,
        opened: true,
        rx_pin: 18,
        tx_pin: 19,
        baud_rate: 9600,
        uart_port: 1,
        mode: "polling",
        resolution: "high",
        units: "metric",
        debug_uart: false,
        poll_interval_ms: 5000,
        rain_clear_delay_ms: 900000,
        daily_reset_enabled: true,
        daily_reset_hour: 0,
        daily_reset_minute: 0,
        last_command: "R",
        last_command_ms: Date.now() - 120,
        last_bytes_written: 2,
        expected_ack: null,
        last_ack: "m",
        last_ack_ms: Date.now() - 160,
        last_raw_response: "Acc 0.00 mm, EventAcc 0.00 mm, TotalAcc 1.24 mm, RInt 0.00 mm/h",
        last_response_ms: Date.now() - 40,
        last_error: null,
        timeouts: 0,
        parse_errors: 0,
        successful_reads: 42,
        response_timeout_ms: 500,
        stale_timeout_ms: 30000,
        last_poll_ms: Date.now() - 40,
        last_poll_age_ms: 40,
        last_rain_detected_ms: Date.now() - 2000,
        last_rain_detected_age_ms: 2000,
        last_total_reset_ms: Date.now() - 3600000,
        last_total_reset_age_ms: 3600000,
        last_reboot_command_ms: null,
        last_reboot_command_age_ms: null,
        last_health_check_ms: Date.now() - 180000,
        last_health_check_age_ms: 180000,
        last_status_line: "SW 1.000 2020.07.06",
        software_version: "1.000",
        software_build_date: "2020.07.06",
        reset_reason: "N",
        power_on_days: 13,
        emitter_1: 9,
        emitter_2: 10,
        emitter_total: 19,
        last_response_age_ms: 40,
        last_successful_read_ms: Date.now() - 40,
        last_successful_read_age_ms: 40,
      },
    },
  },
};

export const mockConfig: Config = {
  deviceName: "SQMeter Demo",
  timezone: "GMT0",
  primaryTimeSource: 0,
  secondaryTimeSource: 1,
  wifi: {
    ssid: "DarkSkyLab",
    password: "",
    hostname: "sqmeter",
    autoReconnect: true,
    reconnectDelayMs: 1000,
    maxReconnectDelayMs: 300000,
  },
  ntp: {
    enabled: true,
    server1: "pool.ntp.org",
    server2: "time.cloudflare.com",
    timezone: "GMT0",
    gmtOffsetSec: 0,
    daylightOffsetSec: 3600,
    syncIntervalMs: 3600000,
  },
  gps: {
    enabled: true,
    rxPin: 16,
    txPin: 17,
    baudRate: 9600,
  },
  mqtt: {
    enabled: true,
    broker: "mqtt.example.com",
    port: 1883,
    username: "",
    password: "",
    topic: "sqmeter/data",
    publishIntervalMs: 60000,
  },
  ota: {
    enabled: false,
    password: "",
  },
  auth: {
    enabled: false,
    username: "admin",
    password: "",
  },
  sensor: {
    readIntervalMs: 5000,
    i2cSDA: 21,
    i2cSCL: 22,
    i2cFrequency: 100000,
  },
  cloudDetection: {
    clearSkyThreshold: -13.0,
    cloudyThreshold: -3.0,
    humidityCorrection: 0.75,
  },
  rain: {
    enabled: true,
    rxPin: 18,
    txPin: 19,
    baudRate: 9600,
    debugUart: false,
    mode: 'polling',
    resolution: 'high',
    units: 'metric',
    pollIntervalMs: 5000,
    rainClearDelayMs: 900000,
    dailyResetEnabled: true,
    dailyResetHour: 0,
    dailyResetMinute: 0,
  },
};

export const mockWifiNetworks = [
  { ssid: "DarkSkyLab", rssi: -42, encryption: "WPA2" },
  { ssid: "NeighbourNet", rssi: -71, encryption: "WPA2" },
  { ssid: "TeleCom_5G", rssi: -85, encryption: "WPA3" },
];

export const mockGithubReleases: GithubRelease[] = [
  {
    tag: "v0.0.3",
    name: "v0.0.3 - RG-15 diagnostics",
    prerelease: false,
    publishedAt: "2026-07-01T12:00:00Z",
    firmwareAssetUrl: "https://github.com/DeanJ87/SQMeter/releases/download/v0.0.3/sqmeter-firmware-v0.0.3.bin",
    firmwareAssetSize: 1273285,
    fsAssetUrl: "https://github.com/DeanJ87/SQMeter/releases/download/v0.0.3/sqmeter-littlefs-v0.0.3.bin",
    fsAssetSize: 274432,
  },
  {
    tag: "v0.0.4-beta.1",
    name: "v0.0.4-beta.1 - Alpaca preview",
    prerelease: true,
    publishedAt: "2026-08-10T09:30:00Z",
    firmwareAssetUrl: "https://github.com/DeanJ87/SQMeter/releases/download/v0.0.4-beta.1/sqmeter-firmware-v0.0.4-beta.1.bin",
    firmwareAssetSize: 1301022,
    fsAssetUrl: "https://github.com/DeanJ87/SQMeter/releases/download/v0.0.4-beta.1/sqmeter-littlefs-v0.0.4-beta.1.bin",
    fsAssetSize: 280100,
  },
];
